# API keys (set the ones you will use)
GROQ_API_KEY=
OPENROUTER_API_KEY=
HF_API_KEY=

# Embeddings model
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Retrieval
TOP_K=5

# CORS origins (comma-separated). Leave empty to allow all, or set explicit list
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,https://lovable.dev,https://lovable.app
